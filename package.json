{
  "name": "@energy8platform/sdk-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Energy8 Platform game SDK monorepo — game-sdk core + stake-bridge",
  "author": "Energy8 Platform",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @energy8platform/game-sdk && npm run build -w @energy8platform/stake-bridge",
    "build:sdk": "npm run build -w @energy8platform/game-sdk",
    "build:stake": "npm run build -w @energy8platform/stake-bridge",
    "clean": "npm run clean -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.0",
    "rollup": "^4.24.0",
    "rollup-plugin-dts": "^6.1.0",
    "tslib": "^2.8.0",
    "typescript": "^5.6.0"
  }
}
